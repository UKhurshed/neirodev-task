import 'package:flutter/material.dart';

class NationalizeScreen extends StatelessWidget {
  const NationalizeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Center(
          child: Text('Nationaliz'),
        ),
      ),
    );
  }
}
